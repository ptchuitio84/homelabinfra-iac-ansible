# =============================================================================
# playbooks/configure_exchange.yml — Exchange Configuration Management
# =============================================================================
# Manages Exchange configuration — receive connectors, accepted domains,
# transport settings. Server already deployed manually.
#
# USAGE:
#   ansible-playbook playbooks/configure_exchange.yml
# =============================================================================

---
- name: Configure Exchange (hmvwapex001.nanonetech.com)
  hosts: hmvwapex001.nanonetech.com
  gather_facts: true

  roles:
    - exchange
