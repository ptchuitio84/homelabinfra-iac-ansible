// =============================================================================
// Jenkinsfile — Pipeline Index
// =============================================================================
// This file is intentionally minimal. All pipeline definitions live in the
// jenkins/ directory, one file per job, named to match the Jenkins job name.
//
// PIPELINE INVENTORY:
// | Job Name                  | File                                | Purpose                        |
// |---------------------------|-------------------------------------|--------------------------------|
// | nnt-jkn-net-backup        | jenkins/nnt-jkn-net-backup.groovy   | Network device config backup   |
//
// HOW TO ADD A NEW PIPELINE:
//   1. Create jenkins/nnt-jkn-{domain}-{action}.groovy
//   2. Add a new Pipeline job in Jenkins pointing at that file
//   3. Add a row to the table above
//
// NAMING CONVENTION:
//   nnt-jkn-{domain}-{action}
//   Domains: net (network), infra (provisioning), mon (monitoring),
//            log (logging), sec (security/AD), plex (Plex)
// =============================================================================
