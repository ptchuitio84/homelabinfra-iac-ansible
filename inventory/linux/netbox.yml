# =============================================================================
# inventory/linux/netbox.yml — NetBox IPAM/DCIM Inventory
# =============================================================================
# NetBox serves as the lab source of truth for IP management and VM inventory.
# Used by Jenkins provisioning pipelines to allocate IPs from predefined
# prefixes before building VMs.
#
# Connection settings:
#   inventory/group_vars/netbox/vars.yml
#   inventory/group_vars/netbox/vault.yml
# =============================================================================

---
netbox:
  hosts:

    # hmvlapntb001 — NetBox IPAM/DCIM
    # IP: 10.10.1.71 | 2vCPU / 4GB RAM / 50GB disk
    hmvlapntb001.nnt.com:
      ansible_host: 10.10.1.71
