---
all:
  children:

    # ── NETWORK DEVICES ──────────────────────────────────────────────────────

    cisco_switches:
      hosts:
        swplnet251.nnt.com:
          ansible_host: 10.100.100.251

    arista_switches:
      hosts:
        swplnet252.nnt.com:
          ansible_host: 10.100.100.252
        # swplnet251_arista:           # uncomment after 7048T-A replaces Cisco
        #   ansible_host: 10.100.100.251

    meraki:
      hosts:
        meraki_mx:
          ansible_host: 10.100.100.254   # Meraki MX LAN IP — adjust if different

    tp_link_poe:
      hosts:
        swplnet253.nnt.com:
          ansible_host: 10.100.100.253   # TP-Link PoE — Omada API if controller present

    # ── COMPUTE ──────────────────────────────────────────────────────────────

    esxi_hosts:
      hosts:
        hmvlesxi001.nnt.com:
          ansible_host: 10.10.1.211
        hmvlesxi002.nnt.com:
          ansible_host: 10.10.1.22
        hmvlesxi003.nnt.com:                   # T710 — add when deployed via PXE
          ansible_host: 10.10.0.33
        hmvlesxi034.nnt.com:                   # R310 — add when deployed via PXE
          ansible_host: 10.10.0.34

    vcenter:
      hosts:
        hmvlapvc001.nnt.com:
          ansible_host: 10.10.0.5

    # ── LINUX VMs ─────────────────────────────────────────────────────────────

    ansible_nodes:
      hosts:
        hmvlapans001.nnt.com:
          ansible_host: 10.10.1.31

    plex_servers:
      hosts:
        plex01:
          ansible_host: 10.100.7.10
          ansible_user: root
