---
# Arista EOS — connection vars
# Collection: arista.eos
# Transport: eAPI (httpapi) — preferred over SSH for EOS automation
# eAPI is already enabled in both switch configs: management api http-commands / no shutdown

ansible_network_os: arista.eos.eos
ansible_connection: ansible.netcommon.httpapi
ansible_httpapi_use_ssl: true
ansible_httpapi_validate_certs: false
ansible_httpapi_port: 443
ansible_user: ptchuitio
ansible_password: "{{ vault_arista_password }}"
ansible_become: true
ansible_become_method: enable
ansible_become_password: "{{ vault_arista_enable_password }}"
