# =============================================================================
# configuration.py — NetBox Configuration
# =============================================================================
# Generated by Ansible — do not edit manually.
# Source: roles/netbox/templates/configuration.py.j2
# =============================================================================

ALLOWED_HOSTS = {{ netbox_allowed_hosts | to_json }}

DATABASE = {
    'ENGINE': 'django.db.backends.postgresql',
    'NAME': '{{ netbox_db_name }}',
    'USER': '{{ netbox_db_user }}',
    'PASSWORD': '{{ vault_netbox_db_password }}',
    'HOST': '{{ netbox_db_host }}',
    'PORT': '',
    'CONN_MAX_AGE': 300,
}

REDIS = {
    'tasks': {
        'HOST': '{{ netbox_redis_host }}',
        'PORT': {{ netbox_redis_port }},
        'PASSWORD': '',
        'DATABASE': 0,
        'SSL': False,
    },
    'caching': {
        'HOST': '{{ netbox_redis_host }}',
        'PORT': {{ netbox_redis_port }},
        'PASSWORD': '',
        'DATABASE': 1,
        'SSL': False,
    }
}

SECRET_KEY = '{{ vault_netbox_secret_key }}'

# =============================================================================
# Optional settings
# =============================================================================

ALLOWED_URL_SCHEMES = (
    'file', 'ftp', 'ftps', 'http', 'https', 'irc', 'mailto', 'sftp', 'ssh',
    'tel', 'telnet', 'tftp', 'vnc', 'xmpp',
)

TIME_ZONE = '{{ netbox_time_zone }}'
LANGUAGE_CODE = '{{ netbox_language_code }}'

LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'handlers': {
        'file': {
            'level': 'INFO',
            'class': 'logging.FileHandler',
            'filename': '{{ netbox_log_dir }}/netbox.log',
        },
    },
    'loggers': {
        'django': {
            'handlers': ['file'],
            'level': 'INFO',
            'propagate': True,
        },
    },
}
