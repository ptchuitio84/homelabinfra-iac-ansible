# =============================================================================
# roles/grafana/handlers/main.yml
# =============================================================================

---
- name: Restart Grafana
  ansible.builtin.systemd:
    name: grafana-server
    state: restarted
    daemon_reload: true
